name: Say hello

on:
  push:
    branches:
      - main


jobs:
  hello_world:
    runs-on: ubuntu-22.04
    steps:
      - name: Echo the message
        run: |
          echo "Hola drogo"
      - name: Listar
        run: ls -l
      - name: Mostrar_Fecha
        run: date